#include<stdio.h>
int main()
{
int i,n,sum=0;
printf("\n Enter number of terms :");
scanf("%d",&n);
printf("\n Even numbers between 0 and %d are :",n);
for(i=1;i<=n;i++)
{
if(i%2==0)
{
printf("%d ",i);
sum=sum+i;
}
}
printf("\n The Sum of all even numbers upto %d=%d",n,sum);
return 0;
}
